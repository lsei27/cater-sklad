{
  "installCommand": "pnpm -C ../.. install --frozen-lockfile --prod=false",
  "buildCommand": "pnpm -C ../.. --filter @cater-sklad/web build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
